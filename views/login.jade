extends wraper

block titulo
	| Login

block content
	if (!isLogged)
		h1 #{title} - Login Page
	else
		h1 #{title}, #{name}!! - Login Page
	if (!isLogged)
		block loginForm
			+loginForm(estrategia)
		h3 #{message}